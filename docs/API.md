# API

- `GET /api/health` — database health and published song count
- `GET /api/songs` — list songs; supports `q`, `voicing`, and `difficulty`
- `GET /api/songs/:id` — retrieve one song and edition metadata
